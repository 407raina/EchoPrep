Attached HTML and CSS Context

<pre class="st-emotion-cache-1m0wf70 e38sfli2"><div style="background-color: transparent;"><code style="white-space: pre;"><span>&lt;div class="feature-grid"&gt;
</span>    &lt;div class="feature-card"&gt;
        &lt;span class="feature-icon"&gt;🎯&lt;/span&gt;
        &lt;h3 class="feature-title"&gt;Personalized Interviews&lt;/h3&gt;
        &lt;p class="feature-description"&gt;AI-powered conversational setup to create tailored mock interviews based on your role and experience level.&lt;/p&gt;
    &lt;/div&gt;

    &lt;div class="feature-card"&gt;
        &lt;span class="feature-icon"&gt;🎙️&lt;/span&gt;
        &lt;h3 class="feature-title"&gt;Voice Interaction&lt;/h3&gt;
        &lt;p class="feature-description"&gt;Real-time speech-to-text and text-to-speech for an authentic interview experience.&lt;/p&gt;
    &lt;/div&gt;

    &lt;div class="feature-card"&gt;
        &lt;span class="feature-icon"&gt;📊&lt;/span&gt;
        &lt;h3 class="feature-title"&gt;AI Feedback&lt;/h3&gt;
        &lt;p class="feature-description"&gt;Detailed performance analysis and constructive feedback to improve your interview skills.&lt;/p&gt;
    &lt;/div&gt;
&lt;/div&gt;</code></div></pre>


/* Matched Rule from user-agent */
pre, xmp, plaintext, listing {
  display: block;
  font-family: monospace;
  white-space-collapse: preserve;
  text-wrap-mode: nowrap;
}

/* Matched Rule from user-agent */
pre, xmp, plaintext, listing {
  margin-block-start: 1em;
  margin-block-end: 1em;
  margin-inline-start: 0px;
  margin-inline-end: 0px;
}

/* Matched Rule from user-agent */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Matched Rule from regular */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Matched Rule from regular */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Matched Rule from regular */
.st-emotion-cache-1m0wf70 {
  height: 100%;
  background: rgb(248, 249, 251);
  border-radius: 0.5rem;
  color: rgb(49, 51, 63);
  font-size: 0.75rem;
  font-family: "Source Code Pro", monospace;
  display: block;
  margin: 0px;
  overflow: auto;
  padding: 1rem 2.3rem 1rem 1rem;
  height: 100%;
  background-image: initial;
  background-position-x: initial;
  background-position-y: initial;
  background-size: initial;
  background-repeat: initial;
  background-attachment: initial;
  background-origin: initial;
  background-clip: initial;
  background-color: rgb(248, 249, 251);
  border-top-left-radius: 0.5rem;
  border-top-right-radius: 0.5rem;
  border-bottom-right-radius: 0.5rem;
  border-bottom-left-radius: 0.5rem;
  color: rgb(49, 51, 63);
  font-size: 0.75rem;
  font-family: "Source Code Pro", monospace;
  display: block;
  margin-top: 0px;
  margin-right: 0px;
  margin-bottom: 0px;
  margin-left: 0px;
  overflow-x: auto;
  overflow-y: auto;
  padding-top: 1rem;
  padding-bottom: 1rem;
  padding-left: 1rem;
  padding-right: 2.3rem;
}

/* Inherited from ancestor level 1 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 1 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 1 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 1 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 1 (regular) */
.st-emotion-cache-1mjeimy {
  height: 100%;
  position: relative;
  margin-left: 0px;
  margin-right: 0px;
  margin-top: 0px;
  height: 100%;
  position: relative;
  margin-left: 0px;
  margin-right: 0px;
  margin-top: 0px;
}

/* Inherited from ancestor level 2 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 2 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 2 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 2 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 2 (regular) */
.st-emotion-cache-acwcvw {
  margin-bottom: 1rem;
  margin-bottom: 1rem;
}

/* Inherited from ancestor level 3 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 3 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 3 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 3 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 3 (regular) */
.main-container {
  background: rgba(250, 253, 214, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  padding: 40px;
  margin: 20px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  background-image: initial;
  background-position-x: initial;
  background-position-y: initial;
  background-size: initial;
  background-repeat: initial;
  background-attachment: initial;
  background-origin: initial;
  background-clip: initial;
  background-color: rgba(250, 253, 214, 0.95);
  backdrop-filter: blur(10px);
  border-top-left-radius: 20px;
  border-top-right-radius: 20px;
  border-bottom-right-radius: 20px;
  border-bottom-left-radius: 20px;
  padding-top: 40px;
  padding-right: 40px;
  padding-bottom: 40px;
  padding-left: 40px;
  margin-top: 20px;
  margin-right: 20px;
  margin-bottom: 20px;
  margin-left: 20px;
  box-shadow: rgba(0, 0, 0, 0.1) 0px 20px 40px;
}

/* Inherited from ancestor level 4 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 4 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 4 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 4 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 4 (regular) */
.st-emotion-cache-r44huj {
  font-family: "Source Sans", sans-serif;
  font-size: 1rem;
  margin-bottom: -1rem;
  color: inherit;
  font-family: "Source Sans", sans-serif;
  font-size: 1rem;
  margin-bottom: -1rem;
  color: inherit;
}

/* Inherited from ancestor level 5 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 5 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 5 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 5 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 6 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 6 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 6 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 6 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 6 (regular) */
.st-emotion-cache-1vo6xi6 {
  width: 100%;
  height: auto;
  max-width: 100%;
  min-width: 1rem;
  position: relative;
  overflow: visible;
  width: 100%;
  height: auto;
  max-width: 100%;
  min-width: 1rem;
  position: relative;
  overflow-x: visible;
  overflow-y: visible;
}

/* Inherited from ancestor level 7 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 7 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 7 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 7 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 7 (regular) */
.st-emotion-cache-tn0cau {
  display: flex;
  gap: 1rem;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-width: 1rem;
  flex-flow: column;
  flex: 1 1 0%;
  -webkit-box-align: stretch;
  align-items: stretch;
  -webkit-box-pack: start;
  justify-content: start;
  display: flex;
  row-gap: 1rem;
  column-gap: 1rem;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-width: 1rem;
  flex-direction: column;
  flex-grow: 1;
  flex-shrink: 1;
  flex-basis: 0%;
  -webkit-box-align: stretch;
  align-items: stretch;
  -webkit-box-pack: start;
  justify-content: start;
  flex-wrap: nowrap;
}

/* Inherited from ancestor level 8 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 8 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 8 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 8 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 8 (regular) */
.st-emotion-cache-zy6yx3 {
  width: 100%;
  padding: 6rem 1rem 10rem;
  max-width: initial;
  min-width: auto;
  width: 100%;
  padding-left: 1rem;
  padding-right: 1rem;
  padding-top: 6rem;
  padding-bottom: 10rem;
  max-width: initial;
  min-width: auto;
}

/* Inherited from ancestor level 8 (regular) */
.st-emotion-cache-zy6yx3 {
  padding-left: 5rem;
  padding-right: 5rem;
  padding-left: 5rem;
  padding-right: 5rem;
}

/* Inherited from ancestor level 9 (user-agent) */
article, aside, footer, header, hgroup, main, nav, search, section {
  display: block;
}

/* Inherited from ancestor level 9 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 9 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 9 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 9 (regular) */
.st-emotion-cache-4rsbii {
  display: flex;
  flex-direction: column;
  width: 100%;
  overflow: auto;
  -webkit-box-align: center;
  align-items: center;
  height: 100dvh;
  display: flex;
  flex-direction: column;
  width: 100%;
  overflow-x: auto;
  overflow-y: auto;
  -webkit-box-align: center;
  align-items: center;
  height: 100dvh;
}

/* Inherited from ancestor level 10 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 10 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 10 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 10 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 10 (regular) */
.st-emotion-cache-6px8kg {
  width: 100%;
  min-width: 0px;
  height: 100dvh;
  width: 100%;
  min-width: 0px;
  height: 100dvh;
}

/* Inherited from ancestor level 10 (regular) */
.st-emotion-cache-6px8kg {
  position: relative;
  position: relative;
}

/* Inherited from ancestor level 11 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 11 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 11 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 11 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 11 (regular) */
.st-emotion-cache-1yiq2ps {
  display: flex;
  flex-direction: row;
  -webkit-box-pack: start;
  place-content: flex-start;
  -webkit-box-align: stretch;
  align-items: stretch;
  position: absolute;
  inset: 0px;
  overflow: hidden;
  display: flex;
  flex-direction: row;
  -webkit-box-pack: start;
  justify-content: flex-start;
  -webkit-box-align: stretch;
  align-items: stretch;
  align-content: flex-start;
  position: absolute;
  top: 0px;
  left: 0px;
  right: 0px;
  bottom: 0px;
  overflow-x: hidden;
  overflow-y: hidden;
}

/* Inherited from ancestor level 12 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 12 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 12 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 12 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 12 (regular) */
.st-emotion-cache-1r4qj8v {
  position: absolute;
  background: rgb(255, 255, 255);
  color: rgb(49, 51, 63);
  inset: 0px;
  color-scheme: light;
  overflow: hidden;
  position: absolute;
  background-image: initial;
  background-position-x: initial;
  background-position-y: initial;
  background-size: initial;
  background-repeat: initial;
  background-attachment: initial;
  background-origin: initial;
  background-clip: initial;
  background-color: rgb(255, 255, 255);
  color: rgb(49, 51, 63);
  top: 0px;
  left: 0px;
  right: 0px;
  bottom: 0px;
  color-scheme: light;
  overflow-x: hidden;
  overflow-y: hidden;
}

/* Inherited from ancestor level 12 (regular) */
.stApp {
  background: linear-gradient(135deg, #647FBC 0%, #91ADC8 100%);
  font-family: 'Crimson Text', serif;
  background-image: linear-gradient(135deg, rgb(100, 127, 188) 0%, rgb(145, 173, 200) 100%);
  background-position-x: initial;
  background-position-y: initial;
  background-size: initial;
  background-repeat: initial;
  background-attachment: initial;
  background-origin: initial;
  background-clip: initial;
  background-color: initial;
  font-family: "Crimson Text", serif;
}

/* Inherited from ancestor level 13 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 13 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 13 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 13 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 14 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 14 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 14 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 14 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 15 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 15 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 15 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 15 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 16 (user-agent) */
body {
  display: block;
  margin-top: 8px;
  margin-right: 8px;
  margin-bottom: 8px;
  margin-left: 8px;
}

/* Inherited from ancestor level 16 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 16 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 16 (regular) */
body {
  counter-reset: katexEqnNo mmlEqnNo;
  counter-reset: katexEqnNo 0 mmlEqnNo 0;
}

/* Inherited from ancestor level 16 (regular) */
body {
  margin: 0px;
  font-family: "Source Sans", sans-serif;
  font-weight: 400;
  line-height: 1.6;
  color: rgb(49, 51, 63);
  background-color: rgb(255, 255, 255);
  text-size-adjust: 100%;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  -webkit-font-smoothing: auto;
  margin-top: 0px;
  margin-right: 0px;
  margin-bottom: 0px;
  margin-left: 0px;
  font-family: "Source Sans", sans-serif;
  font-weight: 400;
  line-height: 1.6;
  color: rgb(49, 51, 63);
  background-color: rgb(255, 255, 255);
  text-size-adjust: 100%;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  -webkit-font-smoothing: auto;
}

/* Inherited from ancestor level 17 (user-agent) */
html {
  display: block;
}

/* Inherited from ancestor level 17 (user-agent) */
:root {
  view-transition-name: root;
}

/* Inherited from ancestor level 17 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 17 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 17 (regular) */
html {
  font-size: 16px;
  font-size: 16px;
}
